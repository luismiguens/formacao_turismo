{% extends 'base.html.twig' %}

{% block body %}
    <h1>Candidatura</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ candidatura.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_candidatura_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_candidatura_edit', { 'id': candidatura.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
