{% extends 'base.html.twig' %}

{% block body %}
    <h1>Votacao</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ votacao.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_votacao_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_votacao_edit', { 'id': votacao.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
